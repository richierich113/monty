#include "monty.h"

/**
 * argument_errprnt - prints error message to stderr when
 * argument passed is not 2
 */
 void argument_errprnt(bool isfile_passed)
 {
 	fprintf(stderr, "USAGE: monty file\n");
 }
